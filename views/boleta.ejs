<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Verificación de Boleta de Pago</title>

  <link rel="stylesheet" href="/css/style.css" />
  <style>
    body{
      background:linear-gradient(135deg,#f8fafc,#eef2ff,#e0e7ff);
      min-height:100vh;
      padding:24px;
      font-family:system-ui,Arial,sans-serif;
    }
    .wrap{ max-width:480px; margin:40px auto; }
    .card{
      background:#fff;
      border-radius:16px;
      box-shadow:0 10px 30px rgba(15,23,42,.18);
      padding:24px 22px 22px;
    }
    .uma-logo{
      width:190px;
      height:auto;
      display:block;
      margin:0 auto 8px;
    }
    .title{
      text-align:center;
      font-weight:800;
      font-size:1.35rem;
      color:#0f172a;
      margin-bottom:4px;
    }
    .subtitle{
      text-align:center;
      color:#6b7280;
      font-size:.92rem;
      margin-bottom:18px;
    }
    .info-row{
      display:flex;
      justify-content:space-between;
      gap:8px;
      margin-bottom:6px;
      font-size:.9rem;
      color:#374151;
    }
    .info-label{
      font-weight:600;
      color:#6b7280;
    }
    .info-value{
      font-weight:700;
      color:#111827;
    }
    .form-group{
      margin-top:16px;
    }
    .form-group label{
      display:block;
      font-weight:700;
      margin-bottom:6px;
      color:#374151;
    }
    .form-group input{
      width:100%;
      border-radius:10px;
      border:1px solid #e5e7eb;
      padding:10px 12px;
      font-size:1rem;
      outline:none;
    }
    .form-group input:focus{
      border-color:#f02454;
      box-shadow:0 0 0 1px #f9a8d4;
    }
    .error-box{
      margin-top:12px;
      background:#fee2e2;
      border:1px solid #fecaca;
      border-radius:10px;
      padding:8px 10px;
      color:#b91c1c;
      font-size:.9rem;
    }
    .actions{
      margin-top:18px;
      display:flex;
      gap:10px;
      justify-content:flex-end;
      flex-wrap:wrap;
    }
    .btn{
      border:none;
      border-radius:10px;
      padding:10px 16px;
      font-weight:800;
      cursor:pointer;
      font-size:.98rem;
    }
    .btn-secondary{
      background:#f3f4f6;
      color:#4b5563;
      border:1px solid #e5e7eb;
    }
    .btn-primary{
      background:#f02454;
      color:#fff;
      border:1px solid #c81e47;
      box-shadow:0 8px 18px rgba(248,113,113,.35);
    }
    .btn-primary:hover{
      filter:brightness(.97);
    }
  </style>
</head>
<body>
  <div class="wrap">
    <div class="card">
      <img src="/images/umalogo1.jpg" alt="UMA" class="uma-logo" />

      <h1 class="title">Verificación de Boleta de Pago</h1>
      <p class="subtitle">
        Hola <strong><%= (firstName + " " + lastName).toUpperCase() %></strong>.<br/>
        Para continuar con la rectificación, ingresa el número de tu boleta de pago.
      </p>

      <div class="info-row">
        <span class="info-label">Código:</span>
        <span class="info-value"><%= studentId %></span>
      </div>
      <div class="info-row">
        <span class="info-label">Periodo:</span>
        <span class="info-value"><%= semester %></span>
      </div>
      <div class="info-row">
        <span class="info-label">DNI:</span>
        <span class="info-value"><%= dni %></span>
      </div>

      <form action="/verify-boleta" method="POST">
        <div class="form-group">
          <label for="boleta">Número de boleta de pago</label>
          <input
            type="text"
            id="boleta"
            name="boleta"
            placeholder="Ejemplo: BB02-076849"
            required
            autocomplete="off"
          />
        </div>

        <% if (error) { %>
          <div class="error-box"><%= error %></div>
        <% } %>

        <div class="actions">
          <a href="/" class="btn btn-secondary" style="text-decoration:none;text-align:center;">Cancelar</a>
          <button type="submit" class="btn btn-primary">Continuar</button>
        </div>
      </form>
    </div>
  </div>
</body>
</html>
