<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Login - UMA</title>
  <link rel="stylesheet" href="/css/style.css">
  <style>
    body, html {
      height: 100%;
      margin: 0;
      padding: 0;
      min-height: 100vh;
    }
    .bg-blur {
      position: fixed;
      top: 0; left: 0; width: 100vw; height: 100vh;
      z-index: 0;
      background: url('/images/uma-building.png') no-repeat center center;
      background-size: cover;
      filter: blur(8px) brightness(0.7);
      opacity: 0.5;
    }
    .login-bgbox {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      width: 410px;
      max-width: 96vw;
      min-height: 420px;
      background: #fff;
      border-radius: 22px;
      box-shadow: 0 8px 32px rgba(0,0,0,0.10);
      z-index: 1;
    }
    .container {
      position: relative;
      z-index: 2;
      max-width: 370px;
      margin: 8% auto;
      background: rgba(255,255,255,0.92);
      border-radius: 14px;
      box-shadow: 0 8px 32px rgba(240,36,84,0.10);
      padding: 36px 28px 28px 28px;
      text-align: center;
    }
    .container h2 {
      color: #f02454;
      margin-bottom: 22px;
      font-weight: 700;
      letter-spacing: 1px;
    }
    .container input {
      width: 100%;
      padding: 12px 10px;
      margin: 10px 0;
      border: 1px solid #f5b3c2;
      border-radius: 7px;
      font-size: 1rem;
      background: #fff;
    }
    .container button {
      width: 100%;
      padding: 12px 0;
      background: #f02454;
      color: #fff;
      border: none;
      border-radius: 7px;
      font-size: 1.1rem;
      font-weight: 600;
      margin-top: 12px;
      cursor: pointer;
      transition: background 0.2s;
    }
    .container button:hover {
      background: #c81e47;
    }
  </style>
</head>
<body>
  <div class="bg-blur"></div>
  <div class="login-bgbox"></div>
  <div class="container">
    <h2>Iniciar Sesión</h2>
    <form action="/login" method="POST">
      <input type="text" name="codigo" placeholder="Código de estudiante" required><br>
      <input type="password" name="dni" placeholder="DNI" required><br>
      <button type="submit">Ingresar</button>
    </form>
  </div>
</body>
</html>
